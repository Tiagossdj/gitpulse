{
  "name": "gitpulse",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "test": "pnpm -r --if-present run test",
    "lint": "pnpm -r lint"
  }
}
