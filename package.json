{
  "name": "gitpulse",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "test": "pnpm -r --if-present run test",
    "lint": "pnpm -r lint",
    "prepare": "husky"
  },
  "lint-staged": {
    "**/*.{ts,tsx}": "biome check --no-errors-on-unmatched"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0"
  }
}
